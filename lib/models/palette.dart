import 'package:flutter/material.dart';

//// This `Palette` class includes all main colors in Movie App
//// Every color can be called just like [Palette.white]

class Palette {
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF12153D);
  static const Color pink = Color(0xFFFE6D8E);
  static const Color yellow = Color(0xFFFCC419);
  static const Color green = Color(0xFF51CF66);
  static const Color grey = Color(0xFF737599);
  static const Color lightGrey = Color(0xFF9A9BB2);
  static const Color transparent = Colors.transparent;
}
